# Tool configuration only. The release version lives in optionwright/__init__.py
# (__version__) so there is exactly one place to bump.

[tool.ruff]
target-version = "py312"
line-length = 110
extend-exclude = ["scripts"]


[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
